This directory contains two scripts that can be used with Nautilus, to use 
these scripts copy them in the directory : ~/.gnome/nautilus-scripts.

* Add_to_archive         : add selected files to an archive.
* Extract_to             : extract archive asking the destination folder.
* Extract_here           : extract archive in the current folder.
* Extract_in_a_subfolder : extract archive in a subfolder with the same name
			   of the archive.
